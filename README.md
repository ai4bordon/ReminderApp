# ReminderApp - Приложение для управления напоминаниями

![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)
![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20Linux%20%7C%20macOS-lightgrey.svg)
![CustomTkinter](https://img.shields.io/badge/GUI-CustomTkinter-darkgreen.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

Современное desktop-приложение для управления напоминаниями с удобным графическим интерфейсом, умными уведомлениями и функцией отсрочки. Создано с использованием Python и CustomTkinter для красивого кроссплатформенного пользовательского интерфейса.

## ✨ Основные функции

### 🎯 Управление напоминаниями
- **Создание напоминаний** с заголовком, описанием и точным временем
- **Визуальный календарный виджет** для выбора даты с навигацией по месяцам
- **Редактирование существующих напоминаний** с полной модификацией полей
- **Удаление ненужных напоминаний** с подтверждением
- **Фильтрация по статусам** (Все, Ожидает, Выполнено, Просрочено, Отменено)
- **Сортировка по времени** (сначала новые/старые)

### 🔔 Умная система уведомлений
- **Многоуровневые уведомления**: CustomTkinter → plyer → console (резерв)
- **Автоматические уведомления** при наступлении времени напоминания
- **Отсрочка активных уведомлений** на 15, 30, 45 минут или 1 час
- **Звуковые сигналы** для важных уведомлений

### ⚡ Быстрые действия
- **Кнопки отсрочки** - автоматически активируются при появлении уведомлений
- **Системный трей** - сворачивание в фоновый режим
- **Автозапуск планировщика** - проверка каждые 300 секунд

### 📊 Статусы напоминаний
- **Ожидает** - активные напоминания, ожидающие выполнения
- **Выполнено** - завершенные задачи, отмеченные как выполненные
- **Просрочено** - пропущенные напоминания (с 2-минутным периодом ожидания)
- **Отменено** - отмененные пользователем напоминания

## 🛠 Системные требования

### Аппаратные требования
- **Python**: версия 3.11 или выше
- **Операционная система**: Windows 10+, macOS 10.14+, Linux (Ubuntu 18.04+)
- **Оперативная память**: минимум 256 МБ
- **Место на диске**: 50 МБ

### Зависимости
```python
customtkinter>=5.2.0    # Современный UI фреймворк
apscheduler>=3.10.0     # Планировщик фоновых задач
plyer>=2.1.0           # Кроссплатформенные уведомления
Pillow>=10.0.0         # Обработка изображений
pystray>=0.19.0        # Поддержка системного трея
```

## 📥 Установка и запуск

### Вариант 1: Готовый исполняемый файл (Windows)
Скачайте готовый файл **`ReminderApp.exe`** из раздела [Releases](../../releases):
- **Установка не требуется** - просто скачайте и запустите
- **Полностью автономный** - работает без Python и зависимостей
- **Размер**: 41.4 МБ standalone executable

### Вариант 2: Запуск из исходного кода

#### 1. Клонирование репозитория
```bash
git clone https://github.com/your-username/ReminderApp.git
cd ReminderApp
```

#### 2. Создание виртуального окружения (рекомендуется)
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

#### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

#### 4. Запуск приложения
```bash
python main.py
```

## 🚀 Использование

### Windows (готовый файл)
Просто дважды щелкните **`ReminderApp.exe`** для запуска приложения.

### Исходный код
```bash
python main.py
```

## 📖 Руководство пользователя

### Первый запуск
1. Запустите `python main.py` или `ReminderApp.exe`
2. База данных `reminders.db` создается автоматически при первом запуске
3. Приложение отображается с пустым списком напоминаний
4. Для создания демонстрационных данных используйте метод `create_demo_data()` в файле [`database.py`](database.py)

### Создание напоминания
1. Нажмите кнопку **"Добавить"**
2. Заполните поля **заголовок** и **описание**
3. Выберите дату в **визуальном календаре**:
   - Используйте кнопки `<` и `>` для навигации между месяцами
   - Кликните на день для выбора
4. Установите время в полях **часы:минуты**
5. Нажмите **"Сохранить"**

### Редактирование напоминания
1. Выберите напоминание в списке (клик по строке)
2. Нажмите кнопку **"Редактировать"**
3. Измените необходимые данные
4. Сохраните изменения

### Работа с уведомлениями
1. При наступлении времени напоминания появляется уведомление
2. **Кнопки отсрочки** автоматически активируются:
   - "Отложить на 15 мин"
   - "Отложить на 30 мин"
   - "Отложить на 45 мин"
   - "Отложить на 1 час"
3. Кнопки отключаются при отсутствии активных уведомлений

### Фильтрация и сортировка
- **Фильтр по статусу**: выпадающее меню в левой панели
- **Сортировка по времени**: переключатель "Сначала новые/старые"
- **Обновление списка**: автоматическое при любых изменениях

## 🏗 Архитектура проекта

```
ReminderApp/
├── main.py                 # Точка входа приложения
├── ui.py                   # GUI интерфейс (CustomTkinter)
├── database.py             # Управление базой данных (SQLite + демо-данные)
├── notifications.py        # Многоуровневая система уведомлений
├── scheduler.py            # Фоновый планировщик (APScheduler)
├── create_icon.py          # Генератор иконки системного трея
├── requirements.txt        # Зависимости Python
├── README.md              # Документация проекта
├── .gitignore             # Правила игнорирования Git
├── assets/
│   └── icon.ico           # Иконка приложения
└── dist/                  # Готовые исполняемые файлы
    └── ReminderApp.exe    # Портативный исполняемый файл Windows
```

### Основные компоненты

#### 1. Database (`database.py`)
- Управление базой данных SQLite
- CRUD операции для напоминаний
- Автоматическое обновление просроченных напоминаний
- Thread-safe операции с блокировками
- Встроенное создание демонстрационных данных

#### 2. UI (`ui.py`)
- Главное окно приложения (CustomTkinter)
- Диалог создания/редактирования с календарным виджетом
- Функциональность системного трея и сворачивания
- Визуальные индикаторы статусов

#### 3. Notifications (`notifications.py`)
- Многоуровневая система доставки уведомлений
- CustomTkinter уведомления (приоритет)
- Системные уведомления через plyer
- Консольные уведомления (резерв)

#### 4. Scheduler (`scheduler.py`)
- Фоновая проверка напоминаний (APScheduler)
- Интервал проверки: 30 секунд
- Автоматическое обновление статусов

## 🆘 Устранение неполадок

### Распространенные проблемы

**"ModuleNotFoundError"**
```bash
# Решение: установить зависимости
pip install -r requirements.txt
```

**"Permission denied" (база данных)**
```bash
# Решение: проверить права доступа к файлам
chmod 755 .
chmod 644 reminders.db  # если файл существует
```

### Проблемы с уведомлениями

**Уведомления не отображаются**
1. Проверьте системные настройки (Windows: Focus Assist, macOS: Do Not Disturb)
2. Резервная система автоматически переключится на консольные уведомления

**Ошибки кодировки (Windows)**
```bash
# Установить UTF-8 кодировку в консоли Python
import sys
sys.stdout.reconfigure(encoding='utf-8')
```

### Проблемы с интерфейсом

**Календарь не отображается**
- Увеличьте размер окна диалога
- Проверьте версию CustomTkinter (>=5.2.0)

**Медленная работа**
- Закройте ненужные приложения
- Проверьте количество напоминаний в базе данных (рекомендуется <1000)

### Сброс базы данных
```bash
# Удалить базу данных и создать новую
rm reminders.db
python main.py  # Создает новую базу данных

# Создать демонстрационные данные программно
# В консоли Python:
# from database import Database
# db = Database()
# db.create_demo_data()
```

## 📈 Производительность

### Рекомендации
- **База данных**: до 10,000 напоминаний
- **Интервал проверки**: 30 секунд (настраивается в scheduler.py)
- **Использование памяти**: ~50-100 МБ в зависимости от количества напоминаний

### Оптимизация
```python
# В scheduler.py можно изменить интервал:
scheduler.add_job(check_reminders, 'interval', seconds=60)  # каждую минуту
```

## 🤝 Участие в разработке

1. Сделайте Fork репозитория
2. Создайте ветку функций (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для подробностей.

## 👨‍💻 Используемые технологии

Создано с применением современных практик разработки Python:
- **Python 3.11+** - основной язык
- **CustomTkinter** - современный GUI фреймворк
- **SQLite** - легковесная база данных
- **APScheduler** - планировщик фоновых задач
- **Plyer** - кроссплатформенные уведомления

## 🎉 Благодарности

- **CustomTkinter** - за красивый, современный UI фреймворк
- **APScheduler** - за надежное планирование фоновых задач
- **Plyer** - за поддержку кроссплатформенных уведомлений

---

**Версия**: 1.0.0  
**Релиз**: Декабрь 2024